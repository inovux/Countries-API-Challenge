// (string | number, { minimumFractionDigits: number }) => number
// formats a given string or number according to the locale of the browser and given minimumFractionDigits options

// Tests have to be written here

function numberFormatter(val: number | string, minimumFractionDigits: number) {
    console.log({ val, minimumFractionDigits })

    return 0
}

numberFormatter('12321312', 2)
